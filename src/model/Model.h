/*
	Purpose from this file is to cooridinate to which model the input was addressed.
1.  Close to modify and open to modification.
2.  Returns stored data in array.
*/
#include <stdio.h>
#include "user/User.h"

int model() {
    printf(user());
	printf("Hello from hello.h file\n");
	return 0;
}